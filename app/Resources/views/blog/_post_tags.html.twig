{% if not post.tags.empty %}
    <p class="post-tags">
        {% for tag in post.tags %}
            <a href="{{ path('filter_tags', { tagSlug: tag.name })}}">
            <span class="label label-default">
                <i class="fa fa-tag"></i> {{ tag.name }}
            </span>
            </a>
        {% endfor %}
    </p>
{% endif %}
